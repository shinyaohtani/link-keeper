# LinkKeeper — Concept Document

## 一言で

**ブラウザのブックマークを「自分のファイル」として手元に取り戻すためのユーティリティ。**

---

## 背景と動機

### ブラウザのブックマークは壊れている

ブラウザのブックマーク機能は、かつてはシンプルで信頼できるものだった。しかし現在のブラウザは、ブックマークを「クラウド同期される、ブラウザが管理するデータ」として扱い、ユーザーの手から離れた場所に置いてしまった。

Safariのアップデートでサイドバーの挙動が変わる。Chromeのブックマークマネージャが突然UIを刷新する。使い慣れた操作が消え、新しい操作を強制される。ブックマークはユーザーの資産であるにもかかわらず、ブラウザベンダーの都合でその管理方法が変えられる。

これは本質的な問題だ。ブックマークの管理UIがブラウザに組み込まれている限り、ユーザーはブラウザのアップデートに振り回され続ける。

### 機能の固定という価値

LinkKeeperが解決するのは「機能の安定性」という問題である。自分で作り、自分で管理するツールは、誰かのアップデートで壊れることがない。必要な機能が、必要なままの形で、永続的に存在する。これはブラウザの一機能としてではなく、独立したアプリケーションとしてブックマークを管理することで初めて得られる性質だ。

---

## LinkKeeperとは何か

LinkKeeperは、macOSネイティブのブックマーク管理ユーティリティである。

Safariのサイドバーに相当する機能を、独立したウィンドウとして提供する。見た目と操作感はFinderのリスト表示に極めて近い。ブラウザとは完全に独立しており、同期も連携もしない。URLをドラッグ＆ドロップで受け取り、クリックでブラウザに渡す。それだけのことを、確実に、快適に行う。

### LinkKeeperとは何でないか

- **ブラウザではない。** Webページの表示・レンダリングは一切行わない。
- **ブックマーク同期サービスではない。** iCloud、Googleアカウント、いかなるクラウドとも連携しない。データはローカルのJSONファイルとして存在する。
- **ブラウザの拡張機能ではない。** ブラウザのAPIに依存せず、ブラウザのアップデートに影響されない。
- **高機能なブックマークマネージャではない。** タグ付け、全文検索、スクリーンショット保存、AI分類、レコメンデーション——そういった「賢い」機能は意図的に持たない。
- **Safariの代替ではない。** Safariと並んで使う、補助的なユーティリティである。

---

## 設計思想

### 1. Finderという正解

macOSユーザーにとって、階層的なデータを操作する最も自然なUIはFinderである。LinkKeeperはFinderのリスト表示を模倣する。これは妥協ではなく、最適解の選択だ。

- フォルダによる階層整理
- ドラッグ＆ドロップによる移動
- 複数選択
- 右クリックのコンテキストメニュー
- クリックで選択、ダブルクリックで開く
- 選択状態のアイテムをもう一度クリックしてリネーム
- ドラッグ中にフォルダ上でホバーすると展開される

これらはすべてmacOSユーザーが無意識に期待する操作であり、LinkKeeperはそのすべてに対応する。「使い方を覚える」必要がないUIを目指す。

### 2. ドラッグ＆ドロップが中心

LinkKeeperの最も重要なインタラクションは、ドラッグ＆ドロップである。

**入力（ブックマークの追加）:**
SafariのアドレスバーやリンクからURLをドラッグし、LinkKeeperのウィンドウにドロップする。これがブックマーク追加の主要な方法である。ダイアログもフォームも不要。ドラッグして、落とす。それだけ。

**整理（ブックマークの移動）:**
ブックマークやフォルダを掴んで、別のフォルダにドロップする。複数選択して一括移動もできる。フォルダの中にフォルダを入れることもできる。Finderでファイルを整理するのとまったく同じ感覚で操作できる。

**出力（ブックマークの利用）:**
LinkKeeperからURLをドラッグして、ブラウザのタブバーや他のアプリケーションにドロップできる。LinkKeeperは入力だけでなく、出力もドラッグ＆ドロップで行える双方向のハブである。

### 3. 最小限のデータ、最大限の実用

ブックマークに必要な情報は驚くほど少ない。

- **URL** — これがブックマークの本質。
- **タイトル** — URLが何であるかを人間が識別するためのラベル。
- **ファビコン** — 視覚的な識別を助ける小さなアイコン。
- **追加日時** — 「いつ保存したか」という唯一の有用なメタデータ。
- **カラータグ** — Finderと同様の、軽量な視覚分類。

これ以上の情報——メモ、タグ、評価、カテゴリ、サムネイル——は意図的に排除している。情報が増えると、管理の手間が増える。管理の手間が増えると、ツールを使わなくなる。LinkKeeperは「保存して、整理して、開く」という最小限のワークフローに集中する。

### 4. 独立性と所有権

LinkKeeperのデータは `~/Library/Application Support/LinkKeeper/bookmarks.json` に保存される、単純なJSONファイルである。

- テキストエディタで読める。
- gitで管理できる。
- cpコマンドでバックアップできる。
- 他のツールで加工できる。

データの形式がシンプルであること、データの場所が明確であること、データへのアクセスに特別なツールが不要であること。これらはすべて「データの所有権がユーザーにある」ことを意味する。クラウドサービスが終了しても、アプリが開発中止になっても、データは残る。

### 5. ウィンドウはサイドバーである

LinkKeeperのウィンドウは、Safariのサイドバーのように縦長で細いことを想定している。デスクトップの端に置き、ブラウザの隣に常駐させる。

フローティングモード（常に最前面）は、この「サイドバー的な使い方」を強化する機能である。ブラウザがフルスクリーンでなければ、LinkKeeperは常に見えている。URLを投げ込みたいときにウィンドウを探す必要がない。

ウィンドウの位置、サイズ、フォルダの展開状態は自動的に保存・復元される。アプリを起動するたびに「前回の続き」から始められる。

### 6. ブラウザに対して中立

LinkKeeperは特定のブラウザに依存しない。右クリックメニューから、インストール済みの任意のブラウザでURLを開くことができる。

Safari、Chrome、Firefox、Edge、Arc、Brave——どのブラウザを使っていても、LinkKeeperは同じように動作する。ブラウザを乗り換えても、ブックマークはLinkKeeperの中に残り続ける。これは「ブラウザから独立したブックマーク」という本プロジェクトの核心的な価値の一つである。

---

## 操作モデル

### クリックの意味論

| 操作 | 動作 |
|---|---|
| シングルクリック | 選択 |
| ダブルクリック | デフォルトブラウザで開く / フォルダの展開・折りたたみ |
| 選択済みアイテムへの再クリック | タイトル編集（Finderのリネームと同じ遅延挙動） |
| 右クリック | コンテキストメニュー（ブラウザ選択、カラータグ、削除等） |

この操作体系は、Finderのそれと完全に一致する。macOSユーザーが新たに学ぶことは何もない。

### キーボード

| キー | 動作 |
|---|---|
| Enter | 選択アイテムのリネーム開始 |
| Delete / Backspace | 選択アイテムの削除 |
| Cmd+N | 新規フォルダ作成 |
| Cmd+F | フローティング切替 |
| Cmd+W | ウィンドウを閉じる |
| Cmd+Q | アプリ終了 |

### ドラッグ＆ドロップの方向

```
Safari (URL)  ──drag──▶  LinkKeeper  ◀──drag──  他のアプリ (URL)
                              │
                              │ drag
                              ▼
                    ブラウザ / 他のアプリ
```

LinkKeeperはURLの「受け皿」であると同時に「供給源」でもある。

---

## 視覚設計

### セルの構成

```
┌─────────────────────────────────────────────────┐
│ [▶] 🌐 ページタイトル              2024/03/15   │
│ [▶] 📁 フォルダ名                  2024/03/10   │
│     🌐 ブックマーク名              2024/03/08   │
└─────────────────────────────────────────────────┘
      ↑    ↑                          ↑
   アイコン タイトル（編集可）       追加日時
```

- **アイコン**: フォルダはシステムフォルダアイコン、ブックマークはファビコン（取得できない場合はグローブアイコン）
- **タイトル**: 編集可能なテキストフィールド。デフォルトはWebページのタイトル
- **追加日時**: 控えめなセカンダリカラーで表示

### カラータグ

Finderと同じ7色（赤・オレンジ・黄・緑・青・紫・グレー）をセルの背景色として表示する。強い色ではなく、薄い透過色。視認性を損なわない程度のアクセント。

カラータグは「意味を持たない分類」である。赤が「重要」を意味するわけではない。ユーザーが自分なりの意味を与える。この自由さがカラータグの価値だ。

---

## データ設計

### 保存形式

```json
[
  {
    "id": "UUID",
    "title": "Example Page",
    "urlString": "https://example.com",
    "faviconData": "<base64 encoded PNG>",
    "dateAdded": "2024-03-15T10:30:00Z",
    "colorTag": 0,
    "children": null,
    "isExpanded": false
  },
  {
    "id": "UUID",
    "title": "My Folder",
    "urlString": null,
    "faviconData": null,
    "dateAdded": "2024-03-10T08:00:00Z",
    "colorTag": 4,
    "children": [
      { ... }
    ],
    "isExpanded": true
  }
]
```

- `children` が `null` → ブックマーク
- `children` が配列（空を含む） → フォルダ
- ツリー構造がそのままJSONのネスト構造に対応する
- ファビコンはPNG形式のBase64データとしてJSONに埋め込まれる
- ファイルサイズは通常のブックマーク使用量（数百〜数千件）では問題にならない

### 保存タイミング

ブックマークの追加・削除・移動・編集・カラータグ変更——あらゆる変更操作の直後に即座に保存する。明示的な「保存」操作は存在しない。データの消失を防ぐ最もシンプルな方法は、常に保存することだ。

---

## 技術的選択とその理由

### macOSネイティブ (AppKit)

SwiftUIではなくAppKit (NSOutlineView) を選択した理由:

- **NSOutlineViewはFinderのリスト表示と同じ系譜のコンポーネントである。** ドラッグ＆ドロップ、複数選択、ドラッグ中のフォルダ自動展開、インライン編集——これらすべてがNSOutlineViewの標準機能として提供される。SwiftUIのOutlineGroupにはこれらの機能が不足している。
- **ドラッグ＆ドロップの精密な制御が可能。** 外部アプリ（Safari）からのドロップ受付、内部でのリオーダー、外部アプリへのドラッグアウト——すべて異なるペーストボードタイプを扱う必要がある。NSOutlineViewDataSourceのドラッグ＆ドロップAPIはこれを過不足なくサポートする。
- **20年以上の実績がある安定したAPI。** AppKitのNSOutlineViewは枯れた技術であり、macOSのメジャーアップデートで壊れるリスクが極めて低い。「機能の固定」を志向するLinkKeeperにとって、これは重要な特性だ。

### XcodeGen

Xcodeプロジェクトファイル (.xcodeproj) を直接管理せず、`project.yml` から生成する。プロジェクト設定の変更がテキストベースで行え、差分の確認が容易。

### 非サンドボックス

App Storeで配布しない個人ツールであるため、サンドボックスを無効にしている。これにより:
- ファイルシステムへの自由なアクセス
- 任意のブラウザの起動
- ネットワークアクセス（ファビコン取得）

が制約なく行える。

---

## 意図的に持たない機能

以下の機能は、検討した上で意図的に排除している。

| 排除した機能 | 理由 |
|---|---|
| ブックマークのインポート/エクスポート | コードの複雑化に対して利用頻度が低い。JSONファイルを直接操作すれば同等のことは可能 |
| 検索 | ブックマークのメタデータ（タイトルとURL）だけでは検索の有用性が低い。フォルダ整理で十分 |
| 自動ソート | 手動配置の自由度を損なう。ユーザー自身の手で並べることに価値がある |
| クラウド同期 | データの独立性・所有権という核心的価値と矛盾する |
| 自動バックアップ | JSONファイルの手動コピーで十分。Time Machineも機能する |
| ブラウザ拡張連携 | ブラウザ依存を生み、アップデートによる破壊のリスクを招く |
| Webページのプレビュー/サムネイル | 技術的に困難であり、データ量が爆発的に増加する |
| メニューバー常駐 | 現時点で必要性が感じられない。ウィンドウのフローティングで十分 |

これらの「ない」は弱さではない。それぞれに理由があり、LinkKeeperのシンプルさを支えている。

---

## 想定する使い方

1. デスクトップの端にLinkKeeperを細長く配置し、フローティングモードにする
2. Safariで気になったページを見つけたら、アドレスバーからURLをドラッグしてLinkKeeperにドロップ
3. ドロップしたブックマークは適当なフォルダに振り分ける（あるいは後でまとめて整理する）
4. 後日、保存したページを開きたくなったらLinkKeeperでダブルクリック
5. 特定のブラウザで開きたい場合は右クリックから選択

「保存」「整理」「開く」——この3つの動作がすべてであり、それぞれが1〜2回のマウス操作で完了する。

---

## 名前の由来

**LinkKeeper** — リンクの番人。

Webの世界ではリンクは儚い。ページは消え、URLは変わり、ブックマークは行き場を失う。LinkKeeperはそれらのリンクを「手元に保つ (keep)」ためのツールである。ブラウザの中で漂うのではなく、自分の管理下に置く。ファイルのように、フォルダのように、確実に。

---

## まとめ

LinkKeeperは、ブラウザのブックマーク管理を「ブラウザの外」に出すことで、安定性・独立性・所有権を取り戻すユーティリティである。

Finderと同じ操作感、ドラッグ＆ドロップ中心のワークフロー、シンプルなJSONベースのデータ保存。機能は最小限に絞り、「壊れない」「変わらない」「自分のもの」であることを最大の価値とする。

複雑なことは何もしない。しかし、やるべきことは完璧にやる。
